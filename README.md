# Housing Price Prediction using Linear Regression and MLPClassifier

Classify residential housing prices into three categories (low / medium / high) using two scikit-learn models: a regularized **Multiple Linear Regression** and a **Multi-Layer Perceptron Classifier (MLPClassifier)**. Performance is compared using 5-fold cross-validation with balanced accuracy.

## Dataset

- **Source:** [Real-Estate And Housing Price Prediction (Kaggle)](https://www.kaggle.com/datasets/abdullahmeo/housing-price-prediction)
- **Size:** ~30 kB, 545 records
- **Columns:** 13 total — 6 integer (`price`, `area`, `bedrooms`, `bathrooms`, `stories`, `parking`), 6 boolean (`mainroad`, `guestroom`, `basement`, `hotwaterheating`, `airconditioning`, `prefarea`), and 1 string (`furnishingstatus`)
- **Target:** `price` — a continuous numeric value, later discretized into 3 class labels

Place the raw CSV at `data/housing_price_data.csv`.

## Preprocessing

Handled by [preprocess.py](preprocess.py). It produces one preprocessed CSV per bin configuration at `data/{key}_housing_price_processed.csv` with the following transformations:

1. **Binary encoding** — the 6 boolean columns are mapped from `yes`/`no` to `1`/`0`.
2. **One-hot encoding** — the `furnishingstatus` column (`furnished`, `semi-furnished`, `unfurnished`) is expanded into three boolean columns.
3. **Target binning** — the continuous `price` is converted into three class labels using thresholds defined in `BIN_CONFIGS[CLASS_THRESHOLD_KEY]` at the top of [preprocess.py](preprocess.py). Several `(low, high)` threshold pairs were explored to balance the three classes; the default **4M / 5.5M** gave the best spread across all three classes. Change `CLASS_THRESHOLD_KEY` to try a different pair.

Running `python preprocess.py` now writes **one preprocessed CSV per bin configuration** to `data/{key}_housing_price_processed.csv` (e.g. `0_housing_price_processed.csv`, `1_housing_price_processed.csv`, …). This lets both `main.py` and the bin-comparison script read whichever configuration they need without rebuilding the data.

## Models

Both models live in their own module and are orchestrated by [main.py](main.py).

### Linear Regression — [models/linear_regression.py](models/linear_regression.py)
- **Model:** `sklearn.linear_model.Ridge` (Multiple Linear Regression with L2 regularization) using default parameters.
- **Classification strategy:** the continuous prediction is bucketed into a class label:
  - prediction `< 0.5` → Class 0
  - `0.5 ≤` prediction `< 1.5` → Class 1
  - prediction `≥ 1.5` → Class 2

### MLPClassifier — [models/mlp_classifier.py](models/mlp_classifier.py)
- **Model:** `sklearn.neural_network.MLPClassifier`, wrapped in a `Pipeline` with `StandardScaler` so features are scaled before entering the network.
- **Architecture:** one hidden layer; number of nodes varied over `{20, 40, 60, 80, 100}`.
- **Learning rates:** `{0.001, 0.01, 0.1}`.
- **Grid:** all 15 (hidden size × learning rate) combinations are evaluated with 5-fold CV; the best config is highlighted in the output table.
- **Classification strategy:** output is already a class label — no conversion needed.

## Experiment

- **Cross-validation:** 5-Fold (≈436 training / 109 testing records per fold).
- **Metric:** **Balanced Accuracy** — chosen because the three price classes are imbalanced, and balanced accuracy averages recall per class so minority-class performance is not drowned out.

## Plots

`main.py` calls the following plotting helpers from the [plots/](plots/) folder:

- **[plots/data_plot.py](plots/data_plot.py)** — three helpers:
  - `plot_bin_distribution_comparison` shows per-class record counts side-by-side for every bin threshold pair explored, so the effect of different `(low, high)` splits on class balance is visible at a glance.
  - `plot_class_distribution` shows how many houses fall into each price class for the active bin config, with counts and percentages.
  - `plot_correlation_heatmap` renders a Pearson correlation heatmap across all features and the target, so you can quickly see which features move with `price_category`.
  All three are called before training to give context on the target distribution and feature relationships.
- **[plots/linear_regression_plot.py](plots/linear_regression_plot.py)** — bar chart of per-fold balanced accuracy with the mean drawn as a horizontal line; the best-performing fold is highlighted in light green.
- **[plots/mlp_classifier_plot.py](plots/mlp_classifier_plot.py)** — grouped bar chart across the full (hidden size × learning rate) grid with error bars for std; the best config is outlined in green.
- **[plots/bin_comparison_plot.py](plots/bin_comparison_plot.py)** — side-by-side bar chart comparing LR (dark red) and MLP-best-config (red) mean balanced accuracy across every `BIN_CONFIGS` entry, with std error bars and the mean value labeled above each bar. Generated by [bin_comparison.py](bin_comparison.py).

## Comparing bin configurations

[bin_comparison.py](bin_comparison.py) runs both models across every entry in `BIN_CONFIGS` and plots the resulting means/stds together, so you can see how the choice of price thresholds affects model performance (and how trading apparent accuracy for a more balanced / reliable evaluation plays out). Run with:

```bash
python bin_comparison.py
```

## Project structure

```
ml_house_price_prediction/
├── data/
│   ├── housing_price_data.csv            # raw dataset (download from Kaggle)
│   └── {key}_housing_price_processed.csv # one per BIN_CONFIGS entry, generated by preprocess.py
├── models/
│   ├── linear_regression.py              # Ridge regression + 5-fold CV
│   └── mlp_classifier.py                 # MLPClassifier grid + 5-fold CV
├── plots/
│   ├── data_plot.py                      # preprocessed-data visualizations (bin distribution comparison, class distribution, heatmap)
│   ├── linear_regression_plot.py         # per-fold accuracy bar chart + mean line
│   ├── mlp_classifier_plot.py            # grid of configs with mean +/- std
│   └── bin_comparison_plot.py            # LR vs MLP mean across all bin configs
├── preprocess.py                         # loads and transforms the raw CSV, writes one CSV per bin config
├── main.py                               # entry point that runs both models for the active bin config
├── bin_comparison.py                     # runs both models across every bin config and plots the comparison
├── requirements.txt
└── README.md
```

## Getting started

### Requirements
- Python 3.9+
- Dependencies listed in [requirements.txt](requirements.txt) — `pandas`, `numpy`, `scikit-learn`, `ipykernel`, `rich`, `matplotlib`

```bash
pip install -r requirements.txt
```

### Run

```bash
# 1. Download the raw CSV from Kaggle and save it to data/housing_price_data.csv
# 2. Train and evaluate the models — main.py auto-runs preprocessing if needed
python main.py
```

> `main.py` checks for the preprocessed CSV for the active `CLASS_THRESHOLD_KEY` (e.g. `data/4_housing_price_processed.csv`) at startup and only runs preprocessing for that key if the file is missing. To regenerate all bin configs (e.g. after editing `preprocess.py`), delete the `data/*_housing_price_processed.csv` files or run `python preprocess.py` directly.

## Author

**Ariana Daka** — ariana.daka@gmail.com, ad2487@njit.edu
